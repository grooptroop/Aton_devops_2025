переходим в cmd


падаем в этот проект

пишем wsl

docker-compose up --build -d

затем проверяем работает ли контейнер

docker ps

переходим по адрессу: localhost


останавливаем контейнер: docker-compose down